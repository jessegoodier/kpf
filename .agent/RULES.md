# Project Rules

1. **Use Current Tools**: When adding features or workflows, always use the latest stable versions of libraries and tools (e.g., `uv`, GitHub Actions).
2. **Workflow Updates**: Ensure GitHub Action versions are pinned to the latest major version (e.g., `@v7` for `setup-uv`).
